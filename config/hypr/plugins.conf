#bind = SUPER, up, hyprexpo:expo, toggle # can be: toggle, off/disable or on/enable
#
#plugin {
#    hyprexpo {
#        columns = 3
#        gap_size = 5
#        bg_col = rgb(111111)
#        workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1
#
#        enable_gesture = true # laptop touchpad, 4 fingers
#        gesture_distance = 300 # how far is the "max"
#        gesture_positive = true # positive = swipe down. Negative = swipe up.
#    }
#}

plugin {
  scroller {
    col.selection_border = rgba(9e1515ff)
    column_default_width = onehalf
    focus_wrap = true
    # ultra-wide monitor
    #column_widths = onefourth onethird onehalf onesixth
    # portrait mode monitors
    #monitor_modes = DP-2=col,HDMI-A-1=col
    gesture_scroll_fingers = 3
    gesture_workspace_switch_prefix = e
    center_active_window = 1
    center_active_column = 1
    center_row_if_space_available = 1
  }
}
